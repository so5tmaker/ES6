class Queue {
    constructor() {
        this.pushStack = new Stack();
        this.popStack = new Stack();
        this.size = 0;
    }

    isEmpty() {
        return this.size === 0;
    }

    push(x) {
        this.pushStack.push(x);
        this.size++;
    }

    pop() {
        if (this.isEmpty()) {
            throw new Error("Очередь пуста");
        }

        if (this.popStack.size() === 0) {
            while (this.pushStack.size() > 0) {
                this.popStack.push(this.pushStack.pop());
            }
        }

        const result = this.popStack.pop();
        this.size--;
        return result;
    }
}
/*
Очередь на двух стеках
Элементы всегда добавляем в первый стек.
Элементы всегда вынимаем из второго стека.
Если второй стек пуст, перекладываем все элементы, пока первый стек не станет пустым.

Операция push(x)

Элементы всегда добавляются в первый стек (pushStack).

    •	Временная сложность: O(1) — операция добавления в стек осуществляется за константное время.

Операция pop()

    1.	Если второй стек (popStack) не пуст, то элемент удаляется из него:
    •	Временная сложность: O(1) — удаление из стека за константное время.
    2.	Если второй стек пуст, все элементы из первого стека перекладываются во второй:
    •	Если в первом стеке было n элементов, то их всех нужно переместить во второй стек.
    •	Временная сложность перекладывания: O(n).

Однако, важно учитывать амортизированную сложность, то есть среднюю сложность всех операций в последовательности вызовов.

Амортизированная временная сложность

Каждый элемент помещается в стек pushStack ровно один раз и перемещается в стек popStack ровно один раз, то есть каждая операция перемещения также происходит ровно один раз. 
Следовательно, общее количество операций по добавлению и перемещению одного элемента в среднем остается постоянным.

Таким образом, амортизированная временная сложность одной операции pop() составляет O(1).

Общая временная сложность:

    •	push(x): O(1)
    •	pop(): O(1) (амортизированная)

Заключение:

Амортизированная временная сложность операций добавления и удаления элемента из очереди на двух стеках равна O(1).
 */